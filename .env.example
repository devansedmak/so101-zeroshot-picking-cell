# copy to .env and fill in — .env is gitignored, NEVER commit it
CYBERWAVE_API_KEY=            # cyberwave.com/profile -> API Tokens
CYBERWAVE_ENVIRONMENT_ID=     # optional, from dashboard environment page
